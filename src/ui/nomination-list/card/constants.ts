const StringKeyBase = 'ui.nomination-list.card';

export const StringKey = {
    location: `${StringKeyBase}.location`,
    intelMap: `${StringKeyBase}.intelMap`,
    bsWatermeter: `${StringKeyBase}.bsWatermeter`,
};