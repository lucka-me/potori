const StringKeyBase = 'ui.dashboard.count-by-month';

export const StringKey = {
    title: `${StringKeyBase}.title`,
    submissions: `${StringKeyBase}.submissions`,
    results: `${StringKeyBase}.results`,
};