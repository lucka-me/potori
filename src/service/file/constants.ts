/**
 * Constants for {@link FileKit}
 */
export default class Constants {
    static readonly type = 'application/json';   // MIME type of json file
    static readonly nominations = 'nominations.json';   // Filename of nomination data
    static readonly nominationsLegacy = 'potori.json';  // Filename of legacy nomination data
    static readonly bsData = 'bsdata.json';      // Filename of Brainstorming data
}